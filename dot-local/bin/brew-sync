#!/usr/bin/env zsh
set -euo pipefail
# alias: brew sync
#:  * `sync`
#:    `brew sync` is an alias for `brew bundle install --cleanup`
brew bundle install --file="${XDG_CONFIG_HOME:-$HOME/.config}/homebrew/Brewfile" --cleanup "$@"
